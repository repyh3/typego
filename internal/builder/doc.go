// Package builder provides shared compilation pipeline components for the TypeGo CLI.
// It consolidates template generation, import scanning, and virtual module creation
// used by both the `build` and `run` commands.
package builder
